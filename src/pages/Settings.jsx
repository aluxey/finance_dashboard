import React from 'react';
import ListeLinks from "../components/common/listLinks";
import '../style/listLinks.css';

const Settings = () => {
    return (
        <div>
            <ListeLinks />
            <h2>Settings</h2>
            <p>Settings page content.</p>
        </div>
    );
};

export default Settings; // Ensure default export
